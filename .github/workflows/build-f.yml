name: Charlotte v0.8.0 APK

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/build-f.yml"
      - "CharlotteAgent-v0.8.0-project.zip"

jobs:
  build-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - uses: android-actions/setup-android@v3

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.9"

      - name: Restore stable signing key
        uses: actions/cache@v4
        with:
          path: ~/.android/debug.keystore
          key: charlotte-v080-debug-keystore-v1

      - name: Unpack project
        run: unzip -q CharlotteAgent-v0.8.0-project.zip

      - name: Install Android tools
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platform-tools" "platforms;android-35" "build-tools;35.0.0"

      - name: Build APK
        run: gradle --no-daemon :app:assembleDebug

      - name: Validate APK
        run: |
          APK="app/build/outputs/apk/debug/app-debug.apk"
          test -f "$APK"
          "$ANDROID_HOME/build-tools/35.0.0/aapt" dump badging "$APK" | tee badging.txt
          grep -q "package: name='com.charlotte.agent.v080'" badging.txt
          grep -q "versionName='0.8.0'" badging.txt
          cp "$APK" CharlotteAgent-v0.8.0-debug.apk
          sha256sum CharlotteAgent-v0.8.0-debug.apk > CharlotteAgent-SHA256.txt

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: CharlotteAgent-v0.8.0
          path: |
            CharlotteAgent-v0.8.0-debug.apk
            CharlotteAgent-SHA256.txt
            badging.txt
          if-no-files-found: error
